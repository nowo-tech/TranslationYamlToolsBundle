nowo_translation_yaml_tools:
    missing_translation_log:
        enabled: true
        web_ui:
            enabled: true
            # Test kernel has no SecurityBundle; opt-in only for fixtures/demos.
            allow_unauthenticated: true
