# Upgrading

## 0.x

Initial development series. Treat minor releases as potentially containing small command output or behaviour tweaks until a stable `1.0.0` is tagged. Read `docs/CHANGELOG.md` before upgrading.
